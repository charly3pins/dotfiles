---
description: Bootstrap Spec-Driven Development context in a project. Detect stack, create openspec/ structure, and build skill registry.
---

# /bootstrap

Delegate to the bootstrap sub-agent.

The sub-agent will:
1. Detect the project tech stack automatically
2. Present findings to you for validation
3. Create the `openspec/` directory structure
4. Build the skill registry
